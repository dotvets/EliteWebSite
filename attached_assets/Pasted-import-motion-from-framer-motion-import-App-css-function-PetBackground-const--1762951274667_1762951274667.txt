import { motion } from "framer-motion";
import "./App.css";

function PetBackground() {
  const shapes = [
    "/assets/shapes/1.svg",
    "/assets/shapes/2.svg",
    "/assets/shapes/3.svg",
  ];

  // دالة لإنشاء حركة عشوائية سلسة
  const randomMotion = () => {
    const xMove = Math.random() * 60 - 30; // بين -30 و +30 بكسل
    const yMove = Math.random() * 60 - 30;
    const rotate = Math.random() * 12 - 6; // دوران خفيف
    return { xMove, yMove, rotate };
  };

  return (
    <div className="absolute inset-0 overflow-hidden pointer-events-none bg-white">
      {Array.from({ length: 20 }).map((_, i) => {
        const src = shapes[i % shapes.length];
        const size = 60 + Math.random() * 100;
        const top = `${Math.random() * 95}%`;
        const left = `${Math.random() * 95}%`;
        const duration = 15 + Math.random() * 10;
        const delay = Math.random() * 5;
        const { xMove, yMove, rotate } = randomMotion();

        return (
          <motion.img
            key={i}
            src={src}
            alt="pet shape"
            className="absolute select-none will-change-transform"
            style={{
              top,
              left,
              width: size,
              height: size,
              opacity: 0.3,
            }}
            animate={{
              x: [0, xMove, -xMove, 0],
              y: [0, yMove, -yMove, 0],
              rotate: [0, rotate, -rotate, 0],
            }}
            transition={{
              duration,
              delay,
              repeat: Infinity,
              repeatType: "mirror",
              ease: "easeInOut",
            }}
          />
        );
      })}
    </div>
  );
}

export default function App() {
  return (
    <div
      className="relative flex items-center justify-center h-screen bg-white text-gray-800 overflow-hidden"
      style={{ fontFamily: "Rubik, sans-serif" }}
    >
      <PetBackground />
      <h1 className="text-3xl font-bold z-10 text-[#8E66AD] drop-shadow-md">
        🐾 Dynamic Random Pet Background
      </h1>
    </div>
  );
}
